// -----------------------------------------------------------------------------
//  Framed Drift  -  UI
//  GDD 0.2  secao 18.1 / 6.4
// -----------------------------------------------------------------------------

using UnityEngine;

namespace FramedDrift.UI
{
    /// <summary>HUD da corrida: DRIFT, COMBO, ANGULO, PERFECT n/m, posicao, progresso.
    ///
    /// O contador principal sobe com tweening, NUNCA salta - e o principal elemento de
    /// espetaculo do jogo (GDD 6.4). O readout de angulo existe porque a camera traseira
    /// encurta o yaw (GDD 19.2): o HUD e uma das duas mitigacoes obrigatorias.</summary>
    public sealed class RaceUI : MonoBehaviour
    {
        // TODO(GDD 18.1 / 6.4): implementar.
    }
}
