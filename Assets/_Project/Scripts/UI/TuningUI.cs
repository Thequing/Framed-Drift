// -----------------------------------------------------------------------------
//  Framed Drift  -  UI
//  GDD 0.2  secao 9
// -----------------------------------------------------------------------------

using UnityEngine;

namespace FramedDrift.UI
{
    /// <summary>8 slots + ajuste fino do diferencial. 4 barras-resumo, 12 stats em hover (GDD 7.3).</summary>
    public sealed class TuningUI : MonoBehaviour
    {
        // TODO(GDD 9): implementar.
    }
}
