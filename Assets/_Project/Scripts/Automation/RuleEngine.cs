// -----------------------------------------------------------------------------
//  Framed Drift  -  Automation
//  GDD 0.2  secao 16.2
// -----------------------------------------------------------------------------

namespace FramedDrift.Automation
{
    /// <summary>Avalia AutomationRules. O auto-equipar roda o simulador em modo rapido com a peca
    /// candidata, N=200 corridas, e compara a media (GDD 16.3).
    ///
    /// Isso e barato (GDD 20.4) e HONESTO: usa o mesmo solucionador do jogo, entao nunca
    /// sugere algo que na pratica piora.</summary>
    public sealed class RuleEngine
    {
        // TODO(GDD 16.2): implementar.
    }
}
